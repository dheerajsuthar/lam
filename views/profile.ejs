<%include _header %>
<% if((currentUser) && (currentUser.id === user.id))%>
<a href="/edit"> Edit your profile</a>
<h1><%= user.name() %></h1>
<h2>Joined on <%= user.createdAt %></h2>
<% if(user.bio) {%>
  <p><%= user.bio%>
  <%}%>
  <% include _footer %>
